package Aula5;

public class printf {
    public static void main(String[] args) {
        /*TIPOS DE DADOS:
         * %c -> char (caracter simples)
         * %s -> String (cadeia de caracteres)
         * %d -> int
         * %i -> int
         * %ld -> long
         * %f -> float ou double
         * %e -> número real em notação científica com o "e" minúsculo (double ou float)
         * %E -> número real em notação científica com o "e" maiúsculo (double ou float)
         * %% -> imprime "%"
         */
    }
}
